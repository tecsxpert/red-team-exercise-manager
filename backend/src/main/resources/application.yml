spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: smtp.gmail.com
    port: 587
    username: santoshchougule541@gmail.com   # 🔥 YOUR REAL EMAIL
    password: cauq mdqj gqwf gqlw        # 🔥 APP PASSWORD (16 chars)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080